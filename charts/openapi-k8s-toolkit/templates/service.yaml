apiVersion: v1
kind: Service
metadata:
  name: {{ include "openapi-k8s-toolkit.fullname" . }}
  labels:
    {{- include "openapi-k8s-toolkit.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "openapi-k8s-toolkit.selectorLabels" . | nindent 4 }}
